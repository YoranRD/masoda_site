---
import { footerSections, siteIdentity, socialLinks } from "../data/site";
import { withBase } from "../lib/links";

const year = new Date().getFullYear();
---

<footer class="site-footer">
  <div class="container footer-grid">
    <div class="footer-intro">
      <p class="eyebrow">Masoda</p>
      <p>
        {siteIdentity.description}
      </p>
      <p class="footer-note">
        Certaines futures sélections boutique pourront inclure des liens affiliés, toujours choisis avec discernement
        et sans surcoût pour la personne qui commande.
      </p>
    </div>

    {
      footerSections.map((section) => (
        <div>
          <h2 class="footer-title">{section.title}</h2>
          <ul class="footer-list">
            {section.links.map((link) => (
              <li>
                <a href={withBase(link.href)}>{link.label}</a>
              </li>
            ))}
          </ul>
        </div>
      ))
    }

    <div>
      <h2 class="footer-title">Réseaux</h2>
      <ul class="footer-list">
        {
          socialLinks.map((link) => (
            <li>
              <a href={link.url} target="_blank" rel="noopener noreferrer">
                {link.label}
              </a>
            </li>
          ))
        }
      </ul>
    </div>
  </div>

  <div class="container footer-bottom">
    <p>© {year} {siteIdentity.legalName}</p>
    <p>Paris · France</p>
  </div>
</footer>

<style>
  .site-footer {
    margin-top: 4rem;
    padding-top: 2.4rem;
    border-top: 1px solid var(--border);
    background:
      linear-gradient(180deg, rgba(248, 243, 235, 0.2) 0%, rgba(242, 233, 220, 0.7) 100%),
      rgba(255, 250, 242, 0.85);
  }

  .footer-grid {
    display: grid;
    grid-template-columns: minmax(0, 1.35fr) repeat(4, minmax(0, 1fr));
    gap: 1.35rem;
  }

  .footer-title {
    margin: 0 0 0.65rem;
    font-size: 1rem;
  }

  .footer-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 0.45rem;
  }

  .footer-list a {
    color: var(--text-soft);
    text-decoration: none;
  }

  .footer-list a:hover {
    color: var(--text);
    text-decoration: underline;
    text-underline-offset: 0.18em;
  }

  .footer-note {
    font-size: 0.95rem;
  }

  .footer-bottom {
    display: flex;
    justify-content: space-between;
    gap: 1rem;
    flex-wrap: wrap;
    margin-top: 2rem;
    padding: 1rem 0 1.4rem;
    border-top: 1px solid rgba(201, 164, 127, 0.45);
    color: var(--text-soft);
    font-size: 0.95rem;
  }

  @media (max-width: 980px) {
    .footer-grid {
      grid-template-columns: 1fr 1fr;
    }
  }

  @media (max-width: 640px) {
    .footer-grid {
      grid-template-columns: 1fr;
    }
  }
</style>

