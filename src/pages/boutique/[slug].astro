---
import CTASection from "../../components/CTASection.astro";
import PageHero from "../../components/PageHero.astro";
import SectionIntro from "../../components/SectionIntro.astro";
import BaseLayout from "../../layouts/BaseLayout.astro";
import { boutiqueCategories } from "../../data/boutique";

export function getStaticPaths() {
  return boutiqueCategories.map((category) => ({
    params: { slug: category.slug },
    props: { category }
  }));
}

const { category } = Astro.props;
---

<BaseLayout
  title={`${category.title} | Boutique Masoda`}
  description={category.summary}
  canonicalPath={`/boutique/${category.slug}/`}
>
  <PageHero
    eyebrow="Boutique en préparation"
    title={category.title}
    intro={category.lead}
    actions={[
      { href: "/boutique/", label: "Retour à la boutique", variant: "secondary" },
      { href: "/contact/", label: "Proposer un partenaire" }
    ]}
    meta={["Curation affiliée", "Page prête"]}
    image={category.image}
    imageAlt={category.imageAlt}
    compact={true}
  />

  <section class="section section-tight">
    <div class="container">
      <SectionIntro
        eyebrow="Axes de curation"
        title="Les critères que cette catégorie devra tenir."
        intro="La page est prête pour accueillir de vraies références, sans inventer des produits avant l'heure."
      />

      <ul class="key-points">
        {category.curationAxes.map((axis) => <li>{axis}</li>)}
      </ul>
    </div>
  </section>

  <section class="section">
    <div class="container">
      <SectionIntro
        eyebrow="Modules placeholder"
        title="Les futures sélections prendront place ici."
        intro="Les cartes produits seront ajoutées quand des références suffisamment cohérentes pourront être recommandées."
      />

      <div class="grid-2">
        {
          category.placeholderModules.map((module) => (
            <article class="surface-card">
              <p class="card-meta">En préparation</p>
              <h3>{module.title}</h3>
              <p>{module.description}</p>
            </article>
          ))
        }
      </div>
    </div>
  </section>

  <CTASection
    eyebrow="Suite logique"
    title="Une base claire avant d'ajouter des partenaires."
    body="Masoda préfère attendre les bonnes références plutôt que remplir la page sans ligne éditoriale."
    actions={[
      { href: "/boutique/", label: "Revenir aux catégories", variant: "secondary" },
      { href: "/contact/", label: "Parler d'une collaboration" }
    ]}
  />
</BaseLayout>
